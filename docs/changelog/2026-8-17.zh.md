## **2026-08-17**

**Aster Code — 新增 Spot Builder 文档**

- 新增 Spot Aster Code 文档，包含 Spot 对接流程、Spot 鉴权方式和 Spot 接口列表。
- 新增 5 个 Spot Builder 接口：`POST /api/v3/approveBuilder`、`POST /api/v3/updateBuilder`、`DELETE /api/v3/delBuilder`、`GET /api/v3/builder`，以及支持 Builder 参数的 `POST /api/v3/order`。
- 更新 `POST /fapi/v3/approveAgent`，新增可选参数 `spotBuilder`、`maxSpotFeeRate`、`spotBuilderName`，支持在授权 Agent 时同时授权 Spot Builder。

## **2026-08-17**

**Aster Code — Spot Builder Documentation Added**

- Added Spot Aster Code documentation, including Spot Integration Flow, Spot Authentication, and Spot Endpoints.
- Added 5 new Spot Builder endpoints: `POST /api/v3/approveBuilder`, `POST /api/v3/updateBuilder`, `DELETE /api/v3/delBuilder`, `GET /api/v3/builder`, and `POST /api/v3/order` (with Builder parameters).
- Updated `POST /fapi/v3/approveAgent` to support simultaneously authorizing a Spot Builder via the new optional parameters: `spotBuilder`, `maxSpotFeeRate`, and `spotBuilderName`.

**Futures V3 API Updates**

- Added **V3 Nonce Mechanism** explanation to General Info
- Added **Noop** endpoint (`POST /fapi/v3/noop`) to Market Data, including Python async demo
- Added **HIDDEN** order type to `timeInForce` enum
- Added **Event: TradePro** WebSocket stream to User Data Streams
- Updated **MARKET_DATA** security type: no longer requires authentication
- Removed `recvWindow` and `timestamp` parameters from all signed endpoints
- Removed **Timing Security** section (recvWindow / timestamp logic)
- Updated Python example code in General Info: new host `fapi3.asterdex.com`, refactored signing functions using `urllib.parse.urlencode`
- Removed `-1021 INVALID_TIMESTAMP` error code

**Futures API Updates**

- Added **Event: TradePro** WebSocket stream to User Data Streams

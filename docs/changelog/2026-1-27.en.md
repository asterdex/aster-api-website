**Aster Code APIs are going live**

**Futures V3 API Updates**

- Added **Get Funding Rate Config** endpoint (`GET /fapi/v3/fundingInfo`) to Market Data
- Updated **Transfer Between Futures And Spot** security type label from `(USER_DATA)` to `(TRANSFER)`
- Updated endpoint label: `POST /fapi/v3/asset/wallet/transfer` from `(HMAC SHA256)` to `(TRANSFER)`
- Added **期货现货互转 (TRANSFER)** section to Chinese documentation

**Futures API Updates**

- Added **Remaining Openable Notional Value** endpoint (`GET /fapi/v1/remainingOpenableNotionalValue`) to Account & Trades
- Updated **Transfer Between Futures And Spot** security type label from `(USER_DATA)` to `(TRANSFER)`
